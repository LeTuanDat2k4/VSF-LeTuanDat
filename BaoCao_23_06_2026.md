# BÁO CÁO TIẾN ĐỘ: KHẮC PHỤC DATA LEAKAGE VÀ TÍCH HỢP MLFLOW TRACKING

* **Ngày lập báo cáo:** 23/06/2026
* **Dự án:** DataThon 2026 Round 1
* **Tác giả:** Lê Tuấn Đạt
* **Mục tiêu:** Kiểm tra và loại bỏ hoàn toàn các lỗi rò rỉ dữ liệu (Data Leakage) trong bộ trích xuất đặc trưng `ReturnFeatureExtractor`, đánh giá hiệu năng mô hình trên phân tách thời gian thực tế (Temporal Split), và tích hợp MLflow để tự động ghi log và đóng gói toàn bộ Pipeline.

---

## 1. Nội dung Công việc Thực hiện

### 1.1. Phát hiện và Khắc phục Data Leakage
Chúng tôi đã viết script kiểm tra [check_leakage.py](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/check_leakage.py) để phân tích sâu chuỗi thời gian của các sự kiện và phát hiện ra các lỗi rò rỉ dữ liệu cực kỳ nghiêm trọng trong logic cũ:
*   **Rò rỉ Đánh giá (Review Date Leakage):** Khách viết review tại `review_date` (trung bình 21 ngày sau đơn hàng). Tuy nhiên, logic cũ gom nhóm và tính điểm đánh giá trung bình lịch sử dựa vào số thứ tự dòng, khiến đánh giá của đơn hàng trước (chưa được viết tại ngày đặt đơn sau) bị rò rỉ vào đặc trưng đơn sau. (Tìm thấy **4.927** đơn bị lỗi).
*   **Rò rỉ Trả hàng & Target Encoding (Return Date Leakage):** Trạng thái trả đơn trước (`is_returned`) được biết tại `return_date` (sau ngày đặt hàng). Logic cũ sử dụng nhãn này trực tiếp tại ngày đặt hàng của đơn tiếp theo khi tính toán tỷ lệ trả hàng lịch sử của khách, ngành hàng, vùng miền, phương thức thanh toán. (Tìm thấy **7.383** đơn bị lỗi).

*   **Giải pháp:** Tái cấu trúc bộ trích xuất đặc trưng `ReturnFeatureExtractor` trong [return_pipeline.py](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/return_pipeline.py):
    1.  Chuyển sang sử dụng `pd.merge_asof` với thiết lập `allow_exact_matches=False`.
    2.  Ép điều kiện thời gian thực tế: Các sự kiện review (`review_date`) và trả hàng (`return_date`) bắt buộc phải xảy ra **trước** ngày đặt hàng hiện tại (`order_date`).
    3.  Lưu thông tin tập huấn luyện trong hàm `fit()` và kết hợp với dữ liệu truyền vào trong `transform()` giúp cho việc suy diễn trên dữ liệu kiểm thử độc lập hoàn toàn không bị mất mát thông tin lịch sử.

### 1.2. Kiểm thử và Đánh giá Leakage-Free
*   **Viết Script Kiểm thử [test_leakage_free.py](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/test_leakage_free.py):** Bằng cách chọn một đơn hàng cụ thể, lấy các đặc trưng trích xuất gốc, sau đó "cố tình cấy" thêm các bản ghi review và return giả lập xảy ra sau đó 1 ngày. Kết quả cho thấy các đặc trưng đầu ra giống nhau 100% $\rightarrow$ **Transformer mới đã hoàn toàn sạch bóng Data Leakage.**
*   **Đánh giá trên Toàn bộ dữ liệu (552.858 dòng):**
    *   Tập huấn luyện (Train): Trước ngày `2022-07-01` (540.277 đơn).
    *   Tập kiểm thử (Test): Từ ngày `2022-07-01` trở đi (12.581 đơn).
    *   **Kết quả:** AUC-ROC Test thực tế đạt **0.5658** (AUC Train đạt **0.6235**). Độ chênh lệch Train-Test rất lành mạnh là **0.0576**. Dù AUC-ROC giảm so với lúc bị rò rỉ dữ liệu (0.6271), đây mới chính là năng lực tổng quát hóa trung thực của mô hình khi triển khai thực tế.

### 1.3. Tích hợp MLflow Tracking
*   **Khắc phục lỗi môi trường:** Sửa lỗi `ImportError: cannot import name 'service' from 'google.protobuf'` bằng cách nâng cấp `mlflow` lên bản `3.14.0`.
*   **Tích hợp ghi log trong [test_pipeline.py](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/test_pipeline.py):**
    *   Sử dụng `with mlflow.start_run()` để bọc toàn bộ quy trình Preprocessing và Training.
    *   Tự động ghi nhận các tham số: `scale_pos_weight`, tỷ lệ chia, hyperparameter của XGBoost.
    *   Ghi nhận các chỉ số: `auc_train`, `auc_test`, `best_iteration`.
    *   Log toàn bộ Scikit-Learn Pipeline bằng `mlflow.sklearn.log_model` kết hợp định dạng `cloudpickle` và truyền kèm `code_paths=["return_pipeline.py"]` để mô hình mang theo định nghĩa của lớp trích xuất đặc trưng tự viết.
*   **Kiểm tra tính toàn vẹn [verify_mlflow_model.py](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/verify_mlflow_model.py):** Viết script tải mô hình tự động từ MLflow run mới nhất, dự đoán trực tiếp thành công trên dữ liệu đơn hàng thô $\rightarrow$ **Xác thực pipeline hoạt động ổn định sau đóng gói.**

---

## 2. Kết quả So sánh Hiệu năng

| Chỉ số | Pipeline Cũ (Có Rò Rỉ) | Pipeline Mới (Không Rò Rỉ) | Ghi chú |
| :--- | :---: | :---: | :--- |
| **AUC-ROC Test** | 0.6271 | **0.5658** | Phản ánh chính xác năng lực thực tế. |
| **AUC-ROC Train** | 0.6295 | **0.6235** | |
| **Chênh lệch Train-Test**| 0.0024 | **0.0576** | Gap thực tế khi không có thông tin tương lai. |
| **Vòng dừng sớm (Early Stop)**| 5 | **71** | Mô hình cần nhiều thời gian học hơn vì đặc trưng sạch. |

---

## 3. Các Tệp Đầu ra Đã Tạo/Cập nhật

| Tên Tệp | Đường Dẫn | Vai Trò |
| :--- | :--- | :--- |
| `return_pipeline.py` | [Link](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/return_pipeline.py) | Cập nhật lớp `ReturnFeatureExtractor` sạch leakage và an toàn thời gian. |
| `test_pipeline.py` | [Link](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/test_pipeline.py) | Cập nhật tích hợp MLflow, tự động ghi log tham số/metrics và model. |
| `check_leakage.py` | [Link](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/check_leakage.py) | Phân tích và phát hiện lỗi rò rỉ dữ liệu trên tập dữ liệu thực tế. |
| `test_leakage_free.py` | [Link](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/test_leakage_free.py) | Kiểm thử bất biến chứng minh transformer không bị rò rỉ. |
| `verify_mlflow_model.py`| [Link](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/verify_mlflow_model.py) | Tải mô hình từ MLflow và dự đoán trực tiếp để kiểm tra đóng gói. |
| `BaoCao_23_06_2026.md` | [Link](file:///c:/Users/PC/Documents/VSF/DataThon/BaoCao_23_06_2026.md) | Báo cáo chi tiết tiến độ công việc ngày 23/06/2026. |

---

## 4. Kế hoạch Tiếp theo
1.  **Feature Engineering bổ sung:** Do hiệu năng thực tế giảm xuống 0.5658 sau khi sửa leakage, cần nghiên cứu thêm các đặc trưng sạch mới (ví dụ: hành vi mua sắm theo mùa vụ, độ nhạy giá thực tế của khách hàng, tương tác chênh lệch size).
2.  **Tuning Hyperparameters:** Tiến hành tối ưu tham số của XGBoost trên tập đặc trưng sạch bằng cách chạy validation chéo (K-Fold) đồng thời log toàn bộ lịch sử thử nghiệm lên MLflow để tìm ra cấu hình tối ưu nhất.
