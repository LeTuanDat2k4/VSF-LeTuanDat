# BÁO CÁO KỸ THUẬT: XÂY DỰNG ĐẶC TRƯNG (FEATURE ENGINEERING) CHO BÀI TOÁN DỰ BÁO TRẢ HÀNG
## KỸ THUẬT TRÍCH XUẤT THÔNG TIN VÀ NGĂN CHẶN RÒ RỈ DỮ LIỆU

* **Ngày lập báo cáo:** 12/06/2026
* **Dự án:** DataThon 2026 Round 1
* **Tác giả:** Lê Tuấn Đạt
* **Mục tiêu:** Xây dựng tập đặc trưng hoàn chỉnh (Feature Dataset) ở cấp độ Đơn hàng (`order_id`) để phục vụ mô hình Machine Learning dự báo nguy cơ trả hàng tại thời điểm Checkout.

---

## 1. Nguyên Tắc Cốt Lõi (Design Principles)

Quá trình Feature Engineering được xây dựng dựa trên các ràng buộc nghiêm ngặt nhằm mô phỏng chính xác luồng dữ liệu thực tế tại thời điểm giỏ hàng được thanh toán:

1. **Đơn vị phân tích:** Đơn hàng (`order_id`). Mỗi dòng dữ liệu đại diện cho toàn bộ giỏ hàng của khách ở một lần mua.
2. **Thời điểm dự báo:** Ngay khi khách hàng bấm **Checkout**.
3. **Cấm rò rỉ dữ liệu (No Data Leakage):** Tuyệt đối loại bỏ các cột thông tin sinh ra sau khi thanh toán như `ship_date`, `delivery_date`, `return_date`, `return_reason`.
4. **Lịch sử tuyến tính (Strict Temporal Order):** Khi tính toán bất kỳ chỉ số lịch sử nào (của khách hàng hoặc của sản phẩm), chỉ được phép sử dụng dữ liệu xảy ra **trước** ngày đặt của đơn hàng hiện tại.

---

## 2. Cấu Trúc Đặc Trưng (Feature Architecture)

Tập dữ liệu cuối cùng bao gồm khoảng **55 đặc trưng** (tổng hợp từ số liệu dạng numeric và categorical), được chia thành 5 nhóm chính:

### Nhóm 1: Thành phần Đơn hàng (Order Composition)
Được "cuộn" (aggregate) từ chi tiết từng sản phẩm (`order_items`) lên cấp đơn hàng.
* **Quy mô và Giá trị:** `n_products` (số loại SP), `n_items` (số lượng SP), `order_total_value`, `avg_unit_price`, `max_unit_price`, `min_unit_price`, `price_range`.
* **Khuyến mãi:** `total_discount`, `has_any_discount`, `discount_item_ratio` (tỷ lệ hàng giảm giá trong đơn), `has_any_promo`.
* **Sự đa dạng (Diversity Indicators):** `n_categories`, `n_segments`, `n_sizes`, `n_colors`. Phản ánh hành vi mua nhiều màu/size để "thử" của khách hàng.
* **Danh mục chủ đạo:** Xác định `dominant_category` (danh mục xuất hiện nhiều nhất) của đơn hàng đó.

### Nhóm 2: Đặc trưng Khách hàng & Hành vi Lịch sử (Customer Features)
Thông tin chân dung tĩnh và chuỗi hành vi mua sắm của khách.
* **Chân dung:** `gender`, `age_group`, `acquisition_channel`, `customer_tenure_days` (thâm niên tại thời điểm mua).
* **Lịch sử mua & trả hàng (Leakage-Safe):** 
  * `customer_order_number`: Số thứ tự của đơn hàng hiện tại (Đơn số 1, 2, hay 3...).
  * `is_first_order`: Flag đánh dấu khách hàng mới mua lần đầu.
  * `customer_return_rate` và `customer_return_count`: Tỷ lệ và số lượng đơn hàng khách đã từng hoàn trả trong quá khứ.
  * `customer_recency_days`: Khoảng thời gian kể từ đơn hàng trước đó.
  * `customer_avg_order_value`: Giá trị trung bình các đơn hàng trong quá khứ.

### Nhóm 3: Đặc trưng Sản phẩm (Product Historical Risk)
* Đánh giá rủi ro cố hữu của các sản phẩm nằm trong giỏ hàng.
* `avg_prod_hist_return_rate`: Trung bình tỷ lệ hoàn trả trong quá khứ của các sản phẩm có trong đơn hàng hiện tại. Nếu giỏ hàng chứa nhiều sản phẩm "tai tiếng" hay bị trả, nguy cơ của cả đơn sẽ tăng cao.

### Nhóm 4 & 5: Kênh giao dịch, Thời gian và Tương tác
* **Kênh và Thiết bị:** `payment_method` (phát hiện từ EDA cho thấy COD có rủi ro cực cao), `device_type`, `order_source`, `region`.
* **Thời gian (Temporal/Cyclical):** `order_month`, `order_day_of_week`, `is_weekend`. Sử dụng Cyclical Encoding (`sin`/`cos`) cho các biến chu kỳ ngày/tháng để mô hình cây có thể bắt được tính liên tục (vd: Tháng 12 nối liền với Tháng 1).
* **Tỷ lệ rủi ro chung (Global Rates):** Trích xuất tỷ lệ trả hàng trung bình của từng danh mục (`category_global_return_rate`), vùng miền, và phương thức thanh toán.

---

## 3. Kỹ Thuật Ngăn Chặn Rò Rỉ Lịch Sử (Leakage-Safe Computation)

Trong quá trình xây dựng các đặc trưng như `customer_return_rate` hay `prod_hist_return_rate`, kỹ thuật **Shifted Cumulative Sum** đã được ứng dụng chặt chẽ:

1. **Sắp xếp theo thời gian (Sort):** Toàn bộ dữ liệu được sắp xếp tuyệt đối theo `customer_id` (hoặc `product_id`) và `order_date`.
2. **Cộng dồn (Cumsum):** Tính tổng số lần trả hàng tích lũy.
3. **Dịch chuyển (Shift 1):** Đây là bước chốt chặn quyết định. Hàm `shift(1)` đẩy kết quả tích lũy xuống 1 hàng, nhằm đảm bảo: Khi tính rủi ro cho Đơn hàng số 5, hệ thống chỉ đếm số lượng trả hàng từ Đơn hàng số 1 đến số 4. Kết quả thực tế của Đơn hàng số 5 bị che khuất hoàn toàn đối với mô hình.

---

## 4. Tổng Kết Output

Quy trình đã thành công cô đọng hàng triệu dòng dữ liệu từ 7 bảng raw (orders, items, products, customers, returns, reviews, geography) thành một ma trận duy nhất ở cấp độ Đơn hàng.

* **Dataset Output:** `return_prediction_features.csv` (Kích thước ~552K đơn hàng x ~55 cột đặc trưng).
* **Config Output:** `return_prediction_feature_config.json` lưu trữ siêu dữ liệu về loại biến (Numeric vs Categorical) và ngày phân tách mốc thời gian (Cutoff Date: `2022-07-01`).
* **Sẵn sàng:** Dữ liệu hoàn toàn sạch (không còn missing values trên các trường số), đã được Label Encoded cho các trường Categorical, và sẵn sàng cắm trực tiếp vào các mô hình LightGBM / XGBoost theo chiến lược Temporal Split.
