# BÁO CÁO TIẾN ĐỘ: TÁI CẤU TRÚC QUY TRÌNH DỰ ĐOÁN TRẢ HÀNG THÀNH SCIKIT-LEARN PIPELINE

* **Ngày lập báo cáo:** 22/06/2026
* **Dự án:** DataThon 2026 Round 1
* **Tác giả:** Lê Tuấn Đạt
* **Mục tiêu:** Tích hợp và đóng gói toàn bộ mã nguồn xử lý đặc trưng (Feature Engineering - từ `return_prediction_fe.ipynb`) và mô hình hóa (Modeling - từ `return_prediction_model.ipynb`) thành một **Scikit-Learn Pipeline** hoàn chỉnh, chuẩn hóa, chống rò rỉ dữ liệu (Data Leakage) và sẵn sàng triển khai thực tế.

---

## 1. Nội dung Công việc Thực hiện

### 1.1. Xây dựng Custom Transformer `ReturnFeatureExtractor`
Do các bước trích xuất đặc trưng của dự án đòi hỏi việc kết hợp dữ liệu từ nhiều bảng quan hệ (`order_items`, `products`, `customers`, `reviews`, `geography`) và tính toán các chỉ số lũy kế động (expanding mean) theo trục thời gian để tránh rò rỉ thông tin tương lai, các bộ chuyển đổi (transformers) mặc định của Scikit-Learn không đáp ứng được.

*   **Giải pháp:** Xây dựng class `ReturnFeatureExtractor` kế thừa `BaseEstimator` và `TransformerMixin`.
*   **Chức năng chính:**
    *   **Order Composition:** Gom nhóm chi tiết đơn hàng, tính toán giá trị đơn, số lượng, chênh lệch giá (`price_range`), tỷ lệ giảm giá (`discount_pct`), tính đa dạng kích thước/màu sắc (`size_variety_ratio`, `color_variety_ratio`).
    *   **Leakage-Safe Product Returns:** Tính toán tỷ lệ trả hàng lịch sử của sản phẩm chỉ dựa trên các đơn hàng phát sinh trước đó.
    *   **Customer & Review History:** Tính toán số đơn hàng lũy kế của khách hàng (`customer_order_number`), thời gian tham gia (`customer_tenure_days`), khoảng cách đơn hàng (`customer_recency_days`), chi tiêu trung bình lịch sử, và điểm đánh giá trung bình lịch sử (`customer_avg_rating`) một cách an toàn thông qua thao tác gom nhóm và dịch chuyển dữ liệu (`cumsum().shift(1)`).
    *   **Leakage-Safe Target Encoding:** Tính toán tỷ lệ hoàn trả lịch sử của ngành hàng (`category_hist_return_rate`), vùng miền (`region_hist_return_rate`), và phương thức thanh toán (`payment_hist_return_rate`).

### 1.2. Định nghĩa Pipeline Thống nhất (`build_pipeline`)
Hàm `build_pipeline` thiết lập một luồng xử lý đầu-cuối (end-to-end):
1.  **Bước 1: Feature Extraction:** Sử dụng `ReturnFeatureExtractor` để biến đổi bảng đơn hàng thô (`df_orders`) cùng các bảng phụ thành tập đặc trưng đầy đủ.
2.  **Bước 2: Column Transformation:**
    *   Các cột dữ liệu số (54 đặc trưng): Xử lý giá trị trống bằng `SimpleImputer(strategy='median')`.
    *   Các cột dữ liệu phân loại (8 đặc trưng): Xử lý giá trị trống bằng `SimpleImputer(strategy='constant', fill_value='unknown')`, sau đó mã hóa nhãn bằng `OrdinalEncoder(handle_unknown='use_encoded_value', unknown_value=-1)`.
3.  **Bước 3: Model Classification:** Tích hợp mô hình `XGBClassifier` với tham số `scale_pos_weight=14.34` để tối ưu hóa khả năng dự đoán trên tập dữ liệu mất cân bằng nghiêm trọng (tỷ lệ trả hàng khoảng 6.5%).

### 1.3. Khắc phục Lỗi Kỹ thuật Môi trường Windows
*   **Vấn đề:** Khi chạy các câu lệnh in ký tự tiếng Việt ra console trên Windows, hệ thống gặp lỗi mã hóa `cp1252`.
*   **Giải pháp:** Thiết lập cấu hình chạy hoặc biến môi trường `PYTHONUTF8=1` để ép Python sử dụng chuẩn UTF-8 khi đọc ghi console, giúp logs và các kết quả hiển thị chính xác các ký tự tiếng Việt.

---

## 2. Kết quả Đánh giá & Xác thực (Verification)

Để kiểm tra tính đúng đắn của pipeline mới, tập lệnh kiểm thử [test_pipeline.py](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/test_pipeline.py) đã được xây dựng và thực thi trên một mẫu ngẫu nhiên gồm 10,000 dòng từ tập dữ liệu đủ điều kiện.

*   **Đường dẫn mã nguồn:**
    *   Pipeline core: [return_pipeline.py](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/return_pipeline.py)
    *   Script test: [test_pipeline.py](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/test_pipeline.py)
*   **Kết quả chạy thử nghiệm:**
    *   Pipeline thực hiện tuần tự và hoàn thành không lỗi các bước `fit` và `predict_proba`.
    *   Chỉ số hiệu năng **AUC-ROC trên tập mẫu đạt: 0.9868** (độ chính xác rất cao trên tập huấn luyện mẫu, chứng minh pipeline đã học đúng các đặc trưng phân tách mạnh mẽ từ dữ liệu lịch sử).

> [!NOTE]
> Chỉ số AUC-ROC cao trên tập mẫu huấn luyện là bình thường do mô hình XGBoost đang khớp tốt dữ liệu. Bước tiếp theo cần đánh giá kiểm thử chéo (Cross-Validation) hoặc Temporal Train/Test Split để có đánh giá khách quan nhất về khả năng tổng quát hóa của mô hình.

---

## 3. Các Tệp Đầu ra Đã Tạo

| Tên Tệp | Đường Dẫn | Vai Trò |
| :--- | :--- | :--- |
| `return_pipeline.py` | [Link](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/return_pipeline.py) | Định nghĩa toàn bộ cấu trúc Scikit-Learn Pipeline cùng lớp biến đổi đặc trưng tùy chỉnh. |
| `test_pipeline.py` | [Link](file:///c:/Users/PC/Documents/VSF/DataThon/return_classify/test_pipeline.py) | Script nạp dữ liệu mẫu, khởi tạo pipeline, huấn luyện và đánh giá AUC-ROC nhanh. |
| `BaoCao_22_06_2026.md` | [Link](file:///c:/Users/PC/Documents/VSF/DataThon/BaoCao_22_06_2026.md) | Báo cáo chi tiết tiến độ công việc ngày 22/06/2026. |

---

## 4. Kế hoạch Tiếp theo
1.  **Huấn luyện diện rộng:** Chạy thử nghiệm Pipeline trên toàn bộ tập dữ liệu (552,858 đơn hàng) thay vì chỉ chạy tập mẫu 10,000 dòng.
2.  **Phân tách tập dữ liệu chuẩn:** Áp dụng Temporal Split (chia theo thời gian trước/sau `2022-07-01`) trên Pipeline để đánh giá hiệu năng thực tế khách quan hơn trên tập kiểm thử độc lập.
3.  **Tập trung Tuning mô hình:** Tinh chỉnh các hyperparameter của XGBoost thông qua GridSearch/RandomSearch kết hợp trực tiếp trên Pipeline để tránh rò rỉ dữ liệu trong quá trình Validation.
