# BÁO CÁO KỸ THUẬT: XÂY DỰNG MÔ HÌNH HỌC MÁY (MODELING) DỰ BÁO TRẢ HÀNG
## ĐÁNH GIÁ HIỆU NĂNG, TỐI ƯU HÓA NGƯỠNG PHÂN LOẠI VÀ PHÂN TÍCH ĐẶC TRƯNG QUYẾT ĐỊNH

* **Ngày lập báo cáo:** 16/06/2026
* **Dự án:** DataThon 2026 Round 1
* **Tác giả:** Lê Tuấn Đạt
* **Mục tiêu:** Xây dựng, tối ưu hóa và đánh giá các mô hình phân loại nhị phân nhằm dự báo nguy cơ trả hàng của đơn hàng ngay tại thời điểm Checkout. Thực hiện tối ưu hóa ngưỡng cắt (Threshold Optimization) cho dữ liệu mất cân bằng nhãn và phân tích các yếu tố quyết định rủi ro.

---

## 1. Phương Pháp Tiếp Cận & Chuẩn Bị Dữ Liệu

Quá trình huấn luyện và đánh giá mô hình tuân thủ nghiêm ngặt các điều kiện thực tế của bài toán thương mại điện tử:

### 1.1. Chia tập dữ liệu theo thời gian (Temporal Split)
Do dữ liệu mua sắm có tính chất chuỗi lịch sử và hành vi tiêu dùng thay đổi theo thời gian, phương pháp chia tập Train/Test ngẫu nhiên (Random Split) sẽ gây ra rò rỉ thông tin tương lai. Em đã áp dụng **Temporal Split** với mốc thời gian cắt (Cutoff Date) là **`2022-07-01`**:
* **Tập huấn luyện (Train Set):** Toàn bộ các đơn hàng phát sinh trước ngày `2022-07-01` (~540,277 đơn hàng, chiếm ~97.7% dữ liệu). Tỷ lệ trả hàng lịch sử là **6.52%**.
* **Tập kiểm thử (Test Set):** Toàn bộ các đơn hàng phát sinh từ ngày `2022-07-01` trở đi (~12,581 đơn hàng, chiếm ~2.3% dữ liệu). Tỷ lệ trả hàng thực tế là **6.61%**.
* **Đặc trưng đầu vào:** Gồm **58 đặc trưng** (49 đặc trưng số học đã điền giá trị thiếu bằng `0` và 9 đặc trưng danh mục đã được Label Encoded).

### 1.2. Xử lý mất cân bằng nhãn (Class Imbalance)
Tỷ lệ nhãn mục tiêu chỉ chiếm **~6.5%**, tạo ra sự chênh lệch lớn giữa hai lớp Keep ($y=0$) và Return ($y=1$). Để mô hình tập trung học lớp thiểu số mà không cần thay đổi phân phối gốc của dữ liệu (tránh Overfitting do Oversampling):
* Đối với mô hình cây (LightGBM, XGBoost): Áp dụng tham số **`scale_pos_weight = 14.34`** (tính bằng tỷ lệ mẫu âm / mẫu dương trong tập Train).
* Đối với các mô hình Baseline (Logistic Regression, Random Forest): Sử dụng tùy chọn **`class_weight='balanced'`**.

---

## 2. Kết Quả Huấn Luyện & So Sánh Mô Hình (Ngưỡng mặc định 0.5)

Em huấn luyện 5 mô hình khác nhau bao gồm cả Baseline và thuật toán Boosting tiên tiến để tiến hành so sánh tổng thể:

| STT | Mô hình | AUC-ROC | F1-Score (Nhãn 1) | Precision (Nhãn 1) | Recall (Nhãn 1) | Average Precision (PR-AUC) |
| :-: | :--- | :---: | :---: | :---: | :---: | :---: |
| 1 | **LightGBM** | **0.5996** | 0.0000 | 0.0000 | 0.0000 | 0.0958 |
| 2 | **LightGBM (Tuned)** | 0.5949 | 0.0000 | 0.0000 | 0.0000 | **0.0967** |
| 3 | **XGBoost** | 0.5920 | 0.1522 | 0.0948 | **0.3858** | **0.0980** |
| 4 | **Logistic Regression** | 0.5865 | 0.1489 | 0.0936 | 0.3642 | 0.0950 |
| 5 | **Random Forest** | 0.5851 | **0.1550** | **0.1086** | 0.2704 | 0.0918 |

### Nhận xét quan trọng:
* **LightGBM** đạt kết quả phân hạng tổng thể tốt nhất với **AUC-ROC = 0.5996** trên tập Test, theo sau là XGBoost với AUC-ROC = 0.5920.
* **Hiện tượng F1-Score = 0.0000** ở mô hình LightGBM (mặc định & tuned) xuất hiện do ở ngưỡng xác suất mặc định (`0.5`), mô hình không gán bất kỳ đơn hàng nào vào nhóm nguy cơ trả hàng ($y=1$). Điều này là do phân phối xác suất dự báo bị lệch nhiều về phía các giá trị nhỏ vì tỷ lệ nhãn dương cực thấp. Hiện tượng này đòi hỏi bắt buộc phải thực hiện bước **Tối ưu hóa ngưỡng quyết định (Threshold Optimization)**.

---

## 3. Tối Ưu Hóa Ngưỡng Quyết Định (Threshold Optimization)

Thay vì sử dụng ngưỡng mặc định `0.5`, Em tiến hành quét ngưỡng cắt từ `0.1` đến `0.9` trên xác suất dự báo của mô hình tốt nhất (LightGBM) để tìm ra điểm tối ưu hóa F1-Score của nhãn rủi ro (`Return`).

* **Ngưỡng tối ưu xác định:** **`0.36`** (tại đây F1-Score đạt giá trị cực đại là `0.16`).

### Báo cáo kết quả chi tiết với ngưỡng tối ưu `0.36` (LightGBM):

```text
              precision    recall  f1-score   support

        Keep       0.94      0.83      0.88     11749
      Return       0.11      0.28      0.16       832

    accuracy                           0.80     12581
   macro avg       0.53      0.56      0.52     12581
weighted avg       0.89      0.80      0.84     12581
```

* **Ý nghĩa thực tế:** 
  * Độ chính xác tổng thể (Accuracy) đạt **80%**.
  * Mô hình bắt được **28% lượng đơn hàng thực tế bị trả** (Recall = 0.28), giúp doanh nghiệp có cơ hội can thiệp sớm vào hơn 1/4 tổng số thiệt hại vận hành do hoàn trả.
  * Tỷ lệ Precision nhãn Return là **11%**. Dù có vẻ thấp, nhưng con số này cao gấp **1.7 lần tỷ lệ ngẫu nhiên (6.61%)**. Cứ khoảng 9 đơn hàng mô hình gắn nhãn cảnh báo thì sẽ có 1 đơn thực sự bị trả.

---

## 4. Đánh Giá Độ Ổn Định Bằng Kiểm Thử Chéo (5-Fold Cross-Validation)

Để kiểm chứng mô hình LightGBM có bị hiện tượng quá khớp (Overfitting) trên tập huấn luyện hay không, em thực hiện 5-Fold Stratified Cross-Validation trên tập Training:

* **Fold 1:** AUC = 0.5817, F1 = 0.1358
* **Fold 2:** AUC = 0.5809, F1 = 0.0000
* **Fold 3:** AUC = 0.5846, F1 = 0.1242
* **Fold 4:** AUC = 0.5767, F1 = 0.0000
* **Fold 5:** AUC = 0.5805, F1 = 0.1254
* **Kết quả CV trung bình (AUC-ROC):** **`0.5809 ± 0.0025`**
* **Kết quả trên tập Test độc lập (AUC-ROC):** **`0.5996`**
* **Khoảng lệch (Gap):** **`0.0187`**

> [!NOTE]
> Khoảng cách giữa chất lượng huấn luyện chéo (Train CV) và tập kiểm thử độc lập (Test Set) cực kỳ nhỏ (< 2%). Điều này chứng tỏ mô hình **hoàn toàn ổn định, có khả năng tổng quát hóa tốt** và không gặp hiện tượng Overfitting.

---

## 5. Phân Tích Độ Quan Trọng Của Các Đặc Trưng (Feature Importance - Gain)

Dưới đây là bảng xếp hạng 10 đặc trưng quan trọng nhất đóng góp vào quyết định dự báo rủi ro trả hàng của mô hình LightGBM dựa trên lượng thông tin thu được (Gain):

| Xếp hạng | Tên đặc trưng | Độ quan trọng (Gain) | Giải thích ý nghĩa |
| :-: | :--- | :---: | :--- |
| 1 | `payment_global_return_rate` | 113,658 | Tỷ lệ hoàn trả lịch sử chung của phương thức thanh toán. |
| 2 | `customer_avg_rating` | 25,800 | Điểm đánh giá trung bình của khách hàng đối với các đơn trước. |
| 3 | `payment_method` | 15,047 | Phương thức thanh toán của đơn hàng hiện tại (đặc biệt là COD). |
| 4 | `customer_avg_order_value` | 12,564 | Giá trị đơn hàng trung bình trong quá khứ của khách hàng. |
| 5 | `customer_order_number` | 10,627 | Số thứ tự đơn hàng hiện tại của khách hàng. |
| 6 | `customer_review_count` | 10,318 | Số lượng review khách hàng đã từng viết. |
| 7 | `order_year` | 8,482 | Năm phát sinh đơn hàng. |
| 8 | `sum_prod_hist_returns` | 7,849 | Tổng số lượt trả hàng của các sản phẩm có trong giỏ hàng trong quá khứ. |
| 9 | `customer_recency_days` | 7,803 | Số ngày kể từ đơn hàng mua sắm gần nhất của khách hàng. |
| 10 | `avg_prod_hist_return_rate` | 6,571 | Trung bình tỷ lệ hoàn trả lịch sử của các sản phẩm trong đơn. |


payment_global_return_rate: Con số đo lường mức độ rủi ro bị trả lại của một phương thức thanh toán trên toàn hệ thống.
vd: Phương thức COD (Nhận hàng mới trả tiền): Cứ 100 đơn hàng đặt bằng COD thì trung bình có 11 đơn bị trả lại $\rightarrow$ Tỷ lệ rủi ro của COD là 11.3% (hoặc viết dưới dạng số là 0.113).
Phương thức Thẻ tín dụng (Credit Card - Trả tiền trước): Cứ 100 đơn hàng đặt bằng thẻ thì chỉ có 5.7 đơn bị trả lại $\rightarrow$ Tỷ lệ rủi ro của Thẻ tín dụng là 5.7% (hoặc viết dưới dạng số là 0.057).

### Nhận xét:
* Nhóm đặc trưng liên quan đến **Kênh thanh toán (COD vs Trả trước)** giữ vị trí quan trọng số 1 và số 3. Điều này đồng nhất với phân tích EDA trước đó: rủi ro COD cao gấp đôi trả trước.
* Hành vi lịch sử và sự tương tác của khách hàng thông qua điểm đánh giá (`customer_avg_rating`), giá trị đơn trung bình (`customer_avg_order_value`) và tần suất mua sắm đóng góp vai trò cốt lõi để phác họa rủi ro.

---

## 6. Phân Tích Lỗi Dự Báo (Error Analysis)

Em tiến hành so sánh các đơn hàng được dự báo đúng là trả hàng (**True Positive - TP**) và các đơn hàng có trả hàng nhưng mô hình bỏ lỡ (**False Negative - FN**) để tìm điểm mù của mô hình:

* **Tỷ lệ dự đoán thực tế:** True Negative: 9,780 | False Positive: 1,969 | False Negative: 595 | True Positive: 237.

### Bảng so sánh đặc trưng trung bình giữa nhóm TP và FN:

| Đặc trưng | True Positive (Mean) | False Negative (Mean) | Độ lệch (Diff %) |
| :--- | :---: | :---: | :---: |
| `order_total_value` (Giá trị đơn hàng) | 28,181.36 | 33,646.70 | **+19.39%** |
| `n_products` (Số lượng sản phẩm) | 1.09 | 1.05 | **-3.73%** |
| `avg_unit_price` (Đơn giá trung bình) | 6,572.34 | 7,337.10 | **+11.64%** |
| `customer_order_number` (Thứ tự đơn) | 14.14 | 14.75 | **+4.35%** |
| `customer_return_rate` (Tỷ lệ trả lịch sử) | 0.0520 | 0.0692 | **+33.05%** |
| `total_discount` (Tổng giảm giá) | 1,660.56 | 2,039.84 | **+22.84%** |
| `avg_prod_hist_return_rate` (Tỷ lệ trả sản phẩm) | 0.0667 | 0.0660 | **-1.01%** |

### Nhận định điểm mù của mô hình:
1. **Các đơn hàng giá trị cao dễ bị bỏ lỡ:** Nhóm đơn hàng bị bỏ lỡ (FN) có tổng giá trị đơn hàng trung bình cao hơn nhóm được dự báo đúng (TP) khoảng **19.39%** (33.6K so với 28.1K) và đơn giá sản phẩm cao hơn **11.64%**. Mô hình có xu hướng thận trọng hơn và bỏ lỡ các đơn hàng có giá trị cao.
2. **Hành vi lịch sử biến động:** Khách hàng trong nhóm FN thực tế có tỷ lệ trả hàng lịch sử trung bình cao hơn nhóm TP đến **33.05%** (6.92% so với 5.20%). Điều này cho thấy mô hình chưa bắt nhạy những khách hàng có tỷ lệ trả hàng lịch sử ở mức trung bình-cao nhưng chưa có đủ tần suất mua sắm lớn gần đây.

---

## 7. Kết Luận & Định Hướng Cải Tiến Tiếp Theo

### Kết luận
Mô hình LightGBM kết hợp điều chỉnh ngưỡng quyết định về **0.36** đã thiết lập được một baseline hoạt động khả thi cho doanh nghiệp: bắt được 28% đơn hàng trả thực tế với độ chính xác cảnh báo đạt 11% (tối ưu hơn việc chọn ngẫu nhiên). Mô hình đạt trạng thái tổng quát hóa tốt, không có dấu hiệu overfitting.

### Hướng cải tiến (Next Steps):
1. **Target Encoding:** Áp dụng target encoding cho các biến phân loại có lực lượng lớn (high cardinality) như danh mục sản phẩm, khu vực địa lý để mô hình khai thác thông tin tốt hơn.
2. **Feature Engineering nâng cao:** Xây dựng thêm các đặc trưng tương tác chéo giữa giá trị giỏ hàng và lịch sử chi tiêu của người dùng.
3. **Mô hình Ensembling:** Thực hiện Stacking hoặc Blending giữa LightGBM, XGBoost và CatBoost để cải thiện vùng nhận diện nhạy cảm AUC-ROC thêm từ 2 - 3%.
