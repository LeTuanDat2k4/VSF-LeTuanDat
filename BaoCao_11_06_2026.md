# BÁO CÁO KỸ THUẬT: PHÂN TÍCH KHÁM PHÁ DỮ LIỆU (EDA) CHO BÀI TOÁN DỰ BÁO TRẢ HÀNG (RETURN PREDICTION)
## ĐỊNH HÌNH BÀI TOÁN, NHẬN DIỆN RÒ RỈ DỮ LIỆU VÀ PHÂN TÍCH HÀNH VI HOÀN TRẢ

* **Ngày lập báo cáo:** 16/06/2026
* **Dự án:** DataThon 2026 Round 1
* **Tác giả:** Lê Tuấn Đạt
* **Mục tiêu:** Chuyển đổi và thiết lập bài toán từ phân tích trả hàng ở cấp sản phẩm sang bài toán **Phân loại nhị phân cấp độ Đơn hàng (Order-level)**. Khám phá các đặc trưng hành vi dẫn đến việc hoàn trả và xây dựng chiến lược ngăn chặn rò rỉ dữ liệu (Data Leakage) tại thời điểm Checkout.

---

## 1. Thiết Lập Khung Bài Toán & Định Nghĩa Biến Mục Tiêu ($y$)

Để có thể đưa ra cảnh báo kịp thời nhằm giảm thiểu tổn thất vận hành, hệ thống dự báo phải hoạt động vào **Thời điểm Khách hàng chốt Đơn (Checkout)**. Do đó, bài toán được định nghĩa lại ở cấp độ **Đơn hàng (Order ID)** thay vì từng sản phẩm lẻ tẻ.

### Tiêu chí lựa chọn dữ liệu (Eligible Orders):
* Chỉ đưa vào phân tích các đơn hàng đã kết thúc chu kỳ mua sắm thực tế, tức là có trạng thái `delivered` (Giao thành công) hoặc `returned` (Đã hoàn trả).
* Loại bỏ các đơn hàng bị hủy (`cancelled`), đang xử lý (`processing`) hoặc đang vận chuyển (`shipped`) vì chưa thể xác định được kết quả cuối cùng, nhằm tránh gây nhiễu nhãn (label noise).

### Định nghĩa Nhãn Mục tiêu ($y$):
* **$y = 1$ (Return):** Đơn hàng có **ít nhất 1 sản phẩm** bị hoàn trả (Order ID xuất hiện trong file `returns.csv`).
* **$y = 0$ (Keep):** Đơn hàng được giao thành công và khách hàng giữ lại toàn bộ sản phẩm.

### Thống kê tổng quan:
* **Tổng số đơn hàng đủ điều kiện:** ~552,858 đơn.
* **Tổng số đơn hàng bị trả ($y=1$):** ~36,062 đơn.
* **Tỷ lệ trả hàng (Return Rate):** Nằm ở mức **~6.5%**. Đây là một bài toán có tập dữ liệu mất cân bằng nhãn khá nặng (Imbalanced Dataset).

---

## 2. Phân Tích Khám Phá (EDA) & Hành Vi Hoàn Trả

Thông qua quá trình EDA, một số Insight quan trọng đã được đúc kết để định hướng cho quá trình Feature Engineering:

### 2.1. Quy mô và Đặc tính của Đơn trả hàng
* Mặc dù một đơn hàng có thể có nhiều sản phẩm, nhưng **gần 90% các đơn hàng bị trả chỉ hoàn trả đúng 1 sản phẩm duy nhất** (Trung vị = 1, Trung bình = 1.11, Max = 3).
* Điều này phản ánh hành vi mua sắm thực tế: Khách hàng thường giữ lại phần lớn đơn hàng và chỉ trả lại món đồ gặp vấn đề cụ thể (như sai màu, không vừa size).
* Việc xảy ra hành vi mua nhiều kích cỡ (`n_sizes > 1`) hoặc nhiều màu (`n_colors > 1`) của cùng một danh mục trong một đơn hàng là một tín hiệu rủi ro cao, báo hiệu khách hàng đang "mua thử để chọn và sẽ trả phần còn lại".

### 2.2. Xu hướng Trả hàng theo Thời gian
* Về mặt số lượng tuyệt đối, số đơn hàng trả lại có sự biến động lớn qua các năm do sự lên xuống của quy mô doanh số tổng.
* Tuy nhiên, **Tỷ lệ trả hàng (Return Rate %)** lại **cực kỳ ổn định** qua suốt 11 năm (2012 - 2022), luôn dao động quanh mức **6.4% - 6.7%**.
* Tính ổn định hệ thống này là một nền tảng vững chắc cho mô hình Machine Learning, đảm bảo không có sự dịch chuyển quá lớn về phân phối mục tiêu (target drift) trong tương lai.

### 2.3. Sự đồng nhất qua Lịch sử mua và Thiết bị
* Khách hàng mới mua lần đầu và khách hàng cũ (Repeat) có tỷ lệ trả hàng y hệt nhau (~6.51% vs 6.53%). Dù là đơn hàng thứ 1 hay thứ 15, tỷ lệ này vẫn giữ nguyên.
* Tương tự, loại thiết bị sử dụng (Desktop, Mobile, Tablet) cũng không tạo ra sự phân hóa đáng kể nào về hành vi (dao động 6.38% - 6.55%).

### 2.4. Giá trị Đơn hàng & Danh mục Sản phẩm
* Phân phối tổng giá trị đơn hàng (Order Total Value) và giá trung bình sản phẩm (Avg Unit Price) của nhóm Keep và nhóm Return gần như xếp chồng lên nhau hoàn toàn. Giá cả không phải là yếu tố tiên quyết dẫn đến quyết định trả hàng.
* Về danh mục sản phẩm chủ đạo (Dominant Category), tỷ lệ trả hàng có sự chênh lệch nhưng không quá lớn (nhóm hàng GenZ cao nhất với 6.68%, Casual thấp nhất với 6.34%).

### 2.5. Tín hiệu dự báo mạnh từ Phương thức Thanh toán (Payment Method)
* Đây là một trong những phát hiện đắt giá nhất của quá trình EDA: **Phương thức COD (Nhận hàng thanh toán) có tỷ lệ trả hàng lên đến 11.33%**, cao gần gấp đôi so với tất cả các phương thức trả trước như Apple Pay, PayPal, Credit Card, Bank Transfer (đều xoay quanh mức 5.7% - 5.8%). 
* Khách hàng dùng COD không bị ràng buộc về tài chính ngay từ lúc chốt đơn, dẫn đến tâm lý từ chối nhận hàng hoặc trả lại thoải mái hơn rất nhiều. Đây sẽ là một **Top Feature** trong mô hình phân loại dự báo.

---

## 3. Rò Rỉ Dữ Liệu (Data Leakage) & Chiến Lược Khắc Phục

Vì mốc thời gian dự báo là **ngay khi Checkout**, việc chặn đứng sự rò rỉ dữ liệu tương lai vào mô hình là tối quan trọng:

* **Tuyệt đối không sử dụng:** Các thông tin xảy ra sau khi bấm đặt hàng như thời gian giao hàng (`delivery_date`), độ trễ giao hàng, ngày trả hàng (`return_date`), lý do trả hàng.
* **Tính toán đặc trưng lịch sử an toàn:** Khi tính toán Lịch sử Trả hàng của khách hàng (Customer Historical Return Rate), chúng ta sử dụng kỹ thuật `cumsum()` kết hợp với `shift(1)`. Thao tác này đảm bảo mô hình chỉ nhìn thấy tỷ lệ trả hàng của khách trong các đơn hàng *trước đó*, hoàn toàn không bị ảnh hưởng bởi nhãn trả hàng của *chính đơn hàng đang xét*.
* **Feature Engineering Cấp Đơn hàng:** Các chi tiết sản phẩm được gom nhóm (aggregate) lên cấp đơn hàng thông qua số lượng mặt hàng (`n_items`), tổng tiền (`order_total_value`), số lượng mã giảm giá (`total_discount`), và sự đa dạng của đơn hàng (`n_categories`, `n_sizes`).

---

## 4. Phương Pháp Đánh Giá Mô Hình & Next Steps

Dựa trên phân tích EDA, quy trình xây dựng Mô hình Học máy (Modeling) được định hướng như sau:

1. **Temporal Split:** Do hành vi là chuỗi thời gian, việc chia Train/Test được thực hiện theo nguyên tắc cắt ngang thời gian (Cutoff Date: `2022-07-01`). Tập Train $\le$ 30/06/2022, Tập Test $\ge$ 01/07/2022.
2. **Xử lý Mất cân bằng Nhãn:** Tỷ lệ $6.5\%$ yêu cầu phải áp dụng kỹ thuật cân bằng trọng số (`scale_pos_weight`) trực tiếp vào hàm mất mát của mô hình (như LightGBM, XGBoost) thay vì lấy mẫu nội suy.
3. **Tiêu chuẩn Đánh giá:** Hủy bỏ Accuracy. Tập trung vào việc tối ưu F1-Score, đo lường AUC-ROC, Average Precision và đặc biệt quan tâm đến giá trị **Recall** thông qua quá trình **Tối ưu Ngưỡng Cắt (Threshold Tuning)**.
